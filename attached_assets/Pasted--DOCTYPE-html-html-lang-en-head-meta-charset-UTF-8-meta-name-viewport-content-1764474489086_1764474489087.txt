<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jelly Mic Toggle</title>
    <style>
        /* Reset & Layout */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #0d0d0d; /* Deepest black */
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
            overflow: hidden;
            perspective: 800px;
        }

        /* Container for the toggle */
        .mic-wrapper {
            position: relative;
            width: 180px; /* Increased size */
            height: 180px;
            display: flex;
            justify-content: center;
            align-items: center;
            transform-style: preserve-3d;
        }

        /* Hide default checkbox */
        .mic-wrapper input {
            opacity: 0;
            width: 0;
            height: 0;
            position: absolute;
        }

        /* The Jelly Button Body */
        .mic-btn {
            position: relative;
            width: 140px; /* Much bigger button */
            height: 140px;
            cursor: pointer;
            border-radius: 50%; /* Make it circular */
            
            /* Default OFF State: Sleek, Metallic Dark */
            background: linear-gradient(145deg, #2e2e2e, #1c1c1c);
            
            /* Glassy/Jelly effects */
            backdrop-filter: blur(4px);
            -webkit-backdrop-filter: blur(4px);
            
            /* Deep shadows for spherical 3D look - Scaled up */
            box-shadow: 
                /* Top-Left Specular Highlight */
                inset 4px 4px 10px rgba(255, 255, 255, 0.15),
                /* Bottom-Right Deep Shadow */
                inset -6px -6px 15px rgba(0, 0, 0, 0.6),
                /* Subtle Rim Light */
                inset 0 0 0 2px rgba(255, 255, 255, 0.05),
                /* Side Wall/Thickness simulation */
                0 10px 0 #0f0f0f,
                /* Ambient Drop Shadow */
                0 25px 40px rgba(0, 0, 0, 0.6);
            
            display: flex;
            justify-content: center;
            align-items: center;
            transition: background 0.4s ease, box-shadow 0.4s ease, transform 0.2s ease;
            transform-style: preserve-3d;
            
            /* Prevent selection */
            user-select: none;
            -webkit-tap-highlight-color: transparent;
        }

        /* Glossy Reflection on top - Curved for Sphere */
        .mic-btn::after {
            content: '';
            position: absolute;
            top: 10px;
            left: 15%;
            right: 15%;
            height: 60px;
            /* Softer, more spherical reflection */
            background: radial-gradient(ellipse at center top, 
                rgba(255, 255, 255, 0.3) 0%, 
                rgba(255, 255, 255, 0.0) 70%);
            border-radius: 50%;
            pointer-events: none;
            filter: blur(1px);
        }

        /* The Microphone Icon (SVG) */
        .mic-icon {
            width: 60px; /* Scaled icon */
            height: 60px;
            fill: #666; /* Dim color when off */
            filter: drop-shadow(0 2px 2px rgba(0,0,0,0.5));
            transition: all 0.4s ease;
            z-index: 5;
            transform: translateZ(10px); /* Float higher above orb */
        }
        
        /* The "Slash" for mute state (CSS Pseudo-element) */
        .slash {
            position: absolute;
            width: 75px; /* Longer slash */
            height: 5px; /* Thicker slash */
            background-color: #ff5555;
            border-radius: 4px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(45deg) translateZ(12px);
            opacity: 1;
            transition: all 0.3s ease;
            box-shadow: 0 3px 6px rgba(0,0,0,0.4);
            pointer-events: none;
        }

        /* --- ACTIVE STATE (ON) --- */

        input:checked + .mic-btn {
            /* Active Jelly Gold Gradient - Brighter Core */
            background: radial-gradient(circle at 30% 30%, #ffeb3b, #ff9800);
            
            /* Glowing Spherical Shadows - Scaled */
            box-shadow: 
                /* Inner Hotspot */
                inset 8px 8px 25px rgba(255, 255, 255, 0.6),
                /* Inner Depth */
                inset -8px -8px 25px rgba(184, 117, 4, 0.4),
                /* Side Wall */
                0 10px 0 #cc7a00, 
                /* Strong Ambient Glow */
                0 30px 60px rgba(255, 152, 0, 0.4), 
                0 0 50px rgba(255, 215, 0, 0.5); /* Outer Halo */
                
            animation: jelly-bop 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        /* Icon styling when ON */
        input:checked + .mic-btn .mic-icon {
            fill: #fff; /* White icon against gold */
            filter: drop-shadow(0 4px 6px rgba(160, 90, 0, 0.5));
            transform: translateZ(10px) scale(1.1);
        }

        /* Hide slash when ON */
        input:checked + .mic-btn .slash {
            width: 0;
            opacity: 0;
        }

        /* Gloss update when ON */
        input:checked + .mic-btn::after {
             background: radial-gradient(ellipse at center top, 
                rgba(255, 255, 255, 0.8) 0%, 
                rgba(255, 255, 255, 0.0) 70%);
        }

        /* Animation for turning OFF (reverse bop) */
        input:not(:checked) + .mic-btn {
            animation: jelly-shrink 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        /* --- PHYSICS ANIMATIONS --- */

        /* Bop: Squash down, then spring up */
        @keyframes jelly-bop {
            0% { transform: scale(1); }
            30% { transform: scale(0.9, 1.1); } /* Tall/Thin (Anticipation) */
            50% { transform: scale(1.1, 0.9) translateY(4px); } /* Squash (Impact) */
            70% { transform: scale(0.95, 1.05) translateY(-2px); } /* Rebound */
            100% { transform: scale(1) translateY(0); }
        }

        @keyframes jelly-shrink {
            0% { transform: scale(1); }
            30% { transform: scale(1.1, 0.9); }
            50% { transform: scale(0.9, 1.1) translateY(4px); }
            70% { transform: scale(1.05, 0.95) translateY(-2px); }
            100% { transform: scale(1) translateY(0); }
        }

        /* Hover effect */
        .mic-wrapper:hover .mic-btn {
            transform: translateY(-2px);
        }
        
    </style>
</head>
<body>

    <label class="mic-wrapper">
        <input type="checkbox" id="micToggle">
        <div class="mic-btn">
            <!-- SVG Mic Icon -->
            <svg class="mic-icon" viewBox="0 0 24 24">
                <path d="M12 14c1.66 0 3-1.34 3-3V5c0-1.66-1.34-3-3-3S9 3.34 9 5v6c0 1.66 1.34 3 3 3z"/>
                <path d="M17 11c0 2.76-2.24 5-5 5s-5-2.24-5-5H5c0 3.53 2.61 6.43 6 6.92V21h2v-3.08c3.39-.49 6-3.39 6-6.92h-2z"/>
            </svg>
            <!-- The Mute Slash -->
            <div class="slash"></div>
        </div>
    </label>

</body>
</html>